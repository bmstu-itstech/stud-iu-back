name: Stud IU Back Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    environment: development

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Install dependencies
        run: uv sync

      - name: Create .env for tests
        run: |
          touch .env
          echo "DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}" >> .env
          echo "DJANGO_DATABASE_HOST=localhost" >> .env
          echo "DJANGO_DATABASE_PORT=5432" >> .env
          echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> .env
          echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> .env
          echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> .env

      - name: Run Ruff (Linter)
        run: uv run ruff check .

  deploy:
    needs: lint
    runs-on: ubuntu-latest
    environment: production
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Deploy to the production
        uses: appleboy/ssh-action@v1.0.3
        env:
          EXTERNAL_HTTP_PORT: ${{ vars.EXTERNAL_HTTP_PORT }}
          PGSSLROOTCERT: ${{ vars.PGSSLROOTCERT }}
          DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
          DJANGO_DATABASE_HOST: ${{ secrets.DJANGO_DATABASE_HOST }}
          DJANGO_DATABASE_PORT: ${{ secrets.DJANGO_DATABASE_PORT }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          DOMAIN_NAME: ${{ vars.DOMAIN_NAME }}
          DJANGO_MEDIA_VOLUME: ${{ vars.DJANGO_MEDIA_VOLUME }}
          DJANGO_STATIC_VOLUME: ${{ vars.DJANGO_STATIC_VOLUME }}
        with:
          host: ${{ vars.SSH_HOST }}
          port: ${{ vars.SSH_PORT }}
          username: ${{ vars.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script_stop: true
          envs: EXTERNAL_HTTP_PORT,PGSSLROOTCERT,DJANGO_SECRET_KEY,DJANGO_DATABASE_HOST,DJANGO_DATABASE_PORT,POSTGRES_USER,POSTGRES_PASSWORD,POSTGRES_DB,DOMAIN_NAME,DJANGO_MEDIA_VOLUME,DJANGO_STATIC_VOLUME
          script: |
            cd ${{ vars.PROJECT_DIR }}
            git stash
            git checkout main
            git pull origin main
            docker compose -f docker-compose.ci.yaml up --build -d
            docker system prune -f